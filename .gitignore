# Build & test artifacts
*.test
# Compiled runner binaries (Go names them after their module)
benchmarks/runners/*/limoni
benchmarks/runners/*/*-runner
*.exe
*.out
*.prof
*.pprof
coverage.txt
coverage.html
/limoni
/ssh_server

# Cargo build output for the Ratatui runner (137 MB after one release build).
# Cargo.lock is deliberately NOT ignored: the runner is a binary crate, and
# pinning its dependency versions is what makes a benchmark run reproducible.
benchmarks/runners/ratatui/target/

# Generated benchmark & platform reports
benchmark-results/
platform-results/

# Local tool / editor state
.claude/
.idea/
.vscode/
settings.json

# Go workspace & vendoring (not used by this module)
go.work
go.work.sum
vendor/

# Generated WebAssembly build output (produced by CI, published to Pages)
examples/wasm/limoni.wasm
site/
